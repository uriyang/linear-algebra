## 6 연속시간 시스템
- 대부분의 물리 현상은 시각 (t) 가 연속인 미분방정식으로 설명된다.
- 이산시간이 "한 단계씩 갱신"되는 모델이라면, 연속시간은 "끊김 없이 계속 변하는" 모델이다.

### 6.1 미분방정식
- 미분방정식은 어떤 값이 **어떻게 변하는지**를 식으로 표현한 것이다.
- 일반 방정식이 보통 "값"을 찾는 문제라면,
- 미분방정식은 "변화 규칙"을 보고 원래 함수나 미래의 움직임을 찾는 문제에 가깝다.

- 나뭇잎 배 예시: 미분방정식 + 행렬로 표현
  ```text
  x1(t) = 가로 방향 위치
  x2(t) = 세로 방향 위치
  ```

- 그래서 시간 (t) 에서의 위치는 벡터로 나타낼 수 있다.
  - x(t) = (x1(t), x2(t))

- 예를 들어 초기 위치가
  - x1(0) = 4
  - x2(0) = 6
  이라면,
  - x(0) = (4, 6)

- 미분방정식
  $$
  (\dfrac{d}{dt}x_1(t)=3x_1(t)-2x_2(t))
  $$
  $$
  (\dfrac{d}{dt}x_2(t)=x_1(t))
  $$

- 이 식은 각각
  - x1의 변화 속도 = 3x1 - 2x2
  - x2의 변화 속도 = x1
  를 뜻한다.

- 즉 현재 위치가 어디냐에 따라 다음 순간의 이동 방향과 속도가 결정된다.

- 처음 위치 ((4,6)) 을 대입하면
  $$
  (\dfrac{d}{dt}x_1(0)=3(4)-2(6)=0)
  $$
  $$
  (\dfrac{d}{dt}x_2(0)=4)
  $$

- 따라서 처음 순간에는 가로 방향으로는 거의 움직이지 않고,
- 세로 방향으로는 위쪽으로 움직인다고 해석할 수 있다.

### 6.2 1차원일 때
- 가장 단순한 형태는
  ```text
  d/dt x(t) = ax(t)
  ```
  이다.

- 이 식은 (x(t)) 의 변화율이 현재 값 (x(t)) 에 비례한다는 뜻이다.
- 즉 현재 값이 클수록 더 빠르게 변하고, 현재 값이 작으면 변화도 작다.

- 예를 들어
  ```text
  d/dt x(t) = 7x(t)
  ```
  라면 변화 속도가 현재 값의 7배라는 의미다.

- 이 미분방정식의 해는
  ```text
  x(t) = e^(7t)x(0)
  ```
  이다.

- 일반적으로는
  ```text
  d/dt x(t) = ax(t)
  x(t) = e^(at)x(0)
  ```
  로 쓸 수 있다.

- 중요한 것은 `a` 의 부호다.
  ```text
  a > 0 : 시간이 지날수록 증가, 폭주 가능
  a = 0 : 변화 없이 유지
  a < 0 : 시간이 지날수록 감소, 수렴
  ```

- 이산시간에서는 `|a|` 를 봤지만,
- 연속시간에서는 `e^(at)` 가 나오므로 `a` 의 **실수부의 부호**가 중요해진다.

### 6.3 대각행렬일 때
- 이번에는 여러 차원의 미분방정식을 본다.
- 행렬 `A` 가 대각행렬이면 각 성분이 서로 섞이지 않아 계산이 단순해진다.

```text
A = ( 5   0   0 )
    ( 0   3   0 )
    ( 0   0  -8 )
```

- 이때
```text
d/dt x1(t) = 5x1(t)
d/dt x2(t) = 3x2(t)
d/dt x3(t) = -8x3(t)
```

- 각각의 해는
```text
x1(t) = x1(0)e^(5t)
x2(t) = x2(0)e^(3t)
x3(t) = x3(0)e^(-8t)
```

- 따라서
```text
5, 3  > 0  → 시간이 지날수록 증가
-8    < 0  → 시간이 지날수록 감소
```

- 즉 연속시간에서는 대각성분의 **부호**를 보면 된다.
- 이 시스템은 5, 3 같은 양의 값이 있으므로 폭주할 위험이 있다.

### 6.4 대각화할 수 있는 경우
- 일반 행렬도 대각화할 수 있으면 대각행렬 문제로 바꾸어 풀 수 있다.
- 즉
  - `A = PDP^-1`
  로 쓸 수 있다면,
- 좌표변환 `y = P^-1 x` 를 통해 복잡한 시스템을 더 쉬운 형태로 바꿀 수 있다.

- 연속시간 시스템
  $$
  \frac{d}{dt}x(t)=Ax(t)
  $$
  는 새 변수 `y` 로 보면
  $$
  \frac{d}{dt}y(t)=Dy(t)
  $$
  가 된다.

- `D` 가 대각행렬이면 각 성분은
  $$
  \frac{d}{dt}y_i(t)=\lambda_i y_i(t)
  $$
  꼴이므로,
  $$
  y_i(t)=e^{\lambda_i t}y_i(0)
  $$
  로 바로 풀 수 있다.

- 즉 대각화가 가능하면, 각 고윳값 방향을 독립적으로 해석할 수 있다.

### 6.5 결론: 고윳값(실수부)의 부호
- 대각화할 수 있는 경우 연속시간 시스템의 거동은 고윳값의 **실수부**로 판단할 수 있다.
  - `Re(λ1), ..., Re(λn)` 중 하나라도 양수이면 폭주
  - `Re(λ1), ..., Re(λn)` 이 모두 0 이하이면 폭주하지 않음

## 7 대각화할 수 없는 경우

### 7.1 먼저 결론
- 행렬 `A` 를 대각화할 수 있으면 각 고윳값을 보고 시스템이 폭주하는지 판단할 수 있다.
- 하지만 `A` 가 항상 대각화되는 것은 아니기 때문에,
- 이 절에서는 **대각화할 수 없는 경우에도 쓸 수 있는 폭주 판정 기준**을 본다.

- 이산시간에서는 고윳값 `λ` 의 절댓값이 중요하다.
  - 고윳값 중 하나라도 `|λ| > 1` 이면 폭주한다.
  - 모든 고윳값이 `|λ| < 1` 이면 폭주하지 않는다.
  - 모든 고윳값이 `|λ| ≤ 1` 이어도, `|λ| = 1` 인 고윳값이 있으면 고윳값만으로는 판단할 수 없다.

- 연속시간에서는 고윳값 `λ` 의 실수부 `Re(λ)` 가 중요하다.
  - 고윳값 중 하나라도 `Re(λ) > 0` 이면 폭주한다.
  - 모든 고윳값이 `Re(λ) < 0` 이면 폭주하지 않는다.
  - 모든 고윳값이 `Re(λ) ≤ 0` 이어도, `Re(λ) = 0` 인 고윳값이 있으면 고윳값만으로는 판단할 수 없다.

- 즉 연속시간에서는 고윳값이 복소평면의 왼쪽 반평면에 있는지를 본다.
```text
Re(λ) < 0 : 안정
Re(λ) > 0 : 불안정
Re(λ) = 0 : 경계, 추가 판단 필요
```

### 7.2 대각까지는 못하더라도 - 요르단 표준형
- 대각화는 안 되더라도, 요르단 표준형으로는 항상 변환할 수 있다.
- 즉 정방행렬 `A` 에 대해 정칙행렬 `P` 를 골라
  - `P^-1AP = J`
  가 요르단 표준형이 되게 할 수 있다.

- 요르단 표준형의 예시
  <img src='./image/chapter4/1.png' width='400'>

- 요르단 표준형은
  - 블록대각행렬이고
  - 각 대각블록은 요르단 셀이다.

- 요르단 셀의 특징
  - 대각성분에는 같은 고윳값이 반복된다.
  - 바로 위 대각선에는 1이 놓인다.

- 즉 대각화는 안 되지만, "거의 대각행렬"에 가까운 표준형으로는 바꿀 수 있다.

### 7.3 요르단 표준형의 성질
- 주요 장점
  - 고윳값 구조가 한눈에 보인다.
  - 고유벡터가 얼마나 부족한지도 드러난다.
  - 거듭제곱이나 행렬지수함수를 구체적으로 계산할 수 있다.

#### 요르단 표준형의 고윳값
- 예를 들어
  - 고윳값 3은 7중복인데 선형독립인 고유벡터는 2개뿐
  - 고윳값 5는 3중복인데 선형독립인 고유벡터는 1개뿐
  이라면,
- 중복도는 큰데 고유벡터 수가 부족하므로 대각화는 불가능하다.

- 즉 대각화 가능 여부는
  - 고윳값이 몇 번 반복되는가
  보다
  - 독립적인 고유벡터가 충분히 있는가
  가 더 중요하다.

#### 요르단 표준형의 거듭제곱
- <img src='./image/chapter4/2.png' width='400'>
- 여기서 `Z` 는 한 칸씩 옆으로 미는 역할을 한다.
- 그래서 요르단 표준형에서는 고윳값뿐 아니라, 요르단 셀의 크기와 연결 구조도 함께 봐야 한다.

- 연속시간에서는 거듭제곱 대신 행렬지수함수
  - `e^(tJ)`
  를 보게 된다.
- 이때도 요르단 셀의 크기가 추가적인 `t, t^2, ...` 항을 만들어 경계 안정성에 영향을 줄 수 있다.

### 7.4 요르단 표준형으로 초깃값 문제를 풀다(폭주 판정의 최종 결론)
- 요르단 표준형에서는 요르단 셀마다 서브 시스템으로 분해된다.
- 따라서
  - 서브 시스템 중 하나라도 폭주의 위험이 있으면 전체도 폭주의 위험이 있다.
  - 모든 서브 시스템이 폭주하지 않으면 전체도 폭주하지 않는다.

#### 이산시간 시스템
- `|λ| > 1` 이면 폭주한다.
- `|λ| = 1` 인 경우는 요르단 셀의 크기 `m` 에 따라 달라진다.
  - `m ≥ 2` 이면 폭주한다.
  - `m = 1` 이면 폭주하지 않는다.
- `|λ| < 1` 이면 폭주하지 않는다.

#### 연속시간 시스템
- `Re(λ) > 0` 이면 폭주한다.
- `Re(λ) = 0` 인 경우는 요르단 셀의 크기 `m` 에 따라 달라진다.
  - `m ≥ 2` 이면 폭주한다.
  - `m = 1` 이면 폭주하지 않는다.
- `Re(λ) < 0` 이면 폭주하지 않는다.

## 8 AI와 연결해서 보면
- 이 장의 핵심은 "반복되는 변환이 시간이 지나며 안정적인가"를 보는 것이다.
- AI에서도 이런 구조는 자주 나온다.
  - RNN의 상태 업데이트
  - 연속시간 모델의 동역학
  - 확산모델이나 Neural ODE의 시간 발전

- 직관적으로 보면
  - 실수부가 큰 양수인 방향은 신호를 키우고
  - 큰 음수인 방향은 빠르게 감쇠시키며
  - 실수부가 0 근처인 방향은 오래 남을 수 있다.

- 그래서 고윳값의 절댓값이나 실수부를 보는 관점은
  - 폭주
  - 소실
  - 경계 안정성
  을 이해하는 데 직접 연결된다.
