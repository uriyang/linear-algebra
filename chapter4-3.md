### 7.5 요르단 표준형 구하는 법
- 요르단 표준형 '구하는 법', '변환 가능성의 증명'

## [가령 구했다면]
### 목표

행렬 \( A \)를 계산하기 쉬운 요르단 표준형 \( J \)로 변환한다.

$$
P^{-1}AP = J
$$

- \(A\): 원래 행렬
- \(J\): 요르단 표준형
- \(P\): 고유벡터/일반화 고유벡터를 모은 행렬

---

### 일반화 고유벡터와 사슬(chain)

고유벡터:

$$
(A-\lambda I)p = 0
$$

일반화 고유벡터:

$$
(A-\lambda I)^k p = 0
$$

책의 예:

$$
(A-3I)p_1 = 0
$$

$$
(A-3I)p_2 = p_1
$$

$$
(A-3I)p_3 = p_2
$$

따라서:

$$
(A-3I)^3 p_3 = 0
$$

즉 \(p_3\)는 일반화 고유벡터.

벡터 흐름:

$$
p_3 \rightarrow p_2 \rightarrow p_1 \rightarrow 0
$$

이 구조를 사슬(chain)이라고 한다.

---

### 사슬과 요르단 블록 관계

- 사슬 1개 ↔ 요르단 블록 1개
- 사슬 길이 ↔ 블록 크기
- 사슬 시작점 ↔ 고유벡터

예:

$$
p_5 \rightarrow p_4 \rightarrow p_3 \rightarrow p_2 \rightarrow p_1 \rightarrow 0
$$

→ 길이 5 사슬  
→ 크기 5 요르단 블록

---

### 요르단 표준형 구하는 순서

고유값 구하기:

$$
\det(\lambda I - A)=0
$$

고유벡터 구하기:

$$
(A-\lambda I)p = 0
$$

부족하면 일반화 고유벡터 구하기:

$$
(A-\lambda I)^k p =0
$$

사슬 구성:

$$
p_3 \rightarrow p_2 \rightarrow p_1 \rightarrow 0
$$

벡터들로 \(P\) 구성:

$$
P=[p_1,p_2,\dots]
$$

요르단 표준형 완성:

$$
P^{-1}AP=J
$$

---

### 핵심 요약

일반화 고유벡터는

$$
(A-\lambda I)^k p = 0
$$

를 만족하는 벡터이고,

이 벡터들을 사슬 형태로 연결하면
요르단 표준형의 블록 구조가 만들어진다.
가령 구했다면
- 가령 구했다면 어떤 성질을 지닐 것인가

## 구하는 법
### 요르단 표준형 구하는 법

#### 고유값 구하기

먼저 행렬 \(A\)의 고유값을 구한다.

$$
\varphi_A(\lambda) = \det(\lambda I - A) = 0
$$

예제에서는 특성방정식이 다음처럼 인수분해된다.

$$
\varphi_A(\lambda) = (\lambda - 3)^7(\lambda - 4)(\lambda - 5)^3
$$

따라서 고유값은 다음과 같다.

- \(\lambda = 3\): 7중해
- \(\lambda = 4\): 1중해
- \(\lambda = 5\): 3중해

---

### 각 고유값에 대응하는 벡터 찾기

고유값별로 고유벡터 또는 일반화 고유벡터를 찾는다.

#### \(\lambda = 4\)

\(\lambda = 4\)는 중해가 아니므로, 일반 고유벡터 하나만 구하면 된다.

$$
(A - 4I)p_8 = 0
$$

따라서 \(p_8\)은 고유값 4에 대응하는 고유벡터다.

---

#### \(\lambda = 5\)

\(\lambda = 5\)는 3중해이다.

만약 고유벡터가 3개 있으면 각각 고유벡터로 두면 되지만,  
여기서는 고유벡터가 1개만 있다고 가정한다.

그래서 크기 3짜리 요르단 셀 하나를 만든다.

$$
(A - 5I)p_9 = 0
$$

$$
(A - 5I)p_{10} = p_9
$$

$$
(A - 5I)p_{11} = p_{10}
$$

즉 흐름은 다음과 같다.

$$
p_{11} \rightarrow p_{10} \rightarrow p_9 \rightarrow 0
$$

이때 \(p_9\)는 고유벡터이고,  
\(p_{10}, p_{11}\)은 일반화 고유벡터다.

---

### 큰 벡터부터 구하는 이유

예를 들어 \(\lambda = 5\)의 경우, 먼저

$$
(A - 5I)^3p_{11} = 0
$$

을 만족하는 \(p_{11} \neq 0\)을 구한다.

그다음 아래처럼 차례대로 구한다.

$$
p_{10} = (A - 5I)p_{11}
$$

$$
p_9 = (A - 5I)p_{10}
$$

이렇게 하면 자연스럽게 사슬이 만들어진다.

---

### 반대로 구하면 안 될까?

반대로

$$
(A - 5I)p_9 = 0
$$

에서 \(p_9\)를 먼저 구하고,  
그다음 \(p_{10}, p_{11}\)을 구할 수도 있다.

하지만 항상 잘 되는 것은 아니다.

왜냐하면 어떤 고유벡터는 더 긴 사슬로 이어지지 않을 수 있기 때문이다.

그래서 책에서는 더 안전한 방법으로,  
가장 긴 사슬의 끝 벡터부터 구하는 방식을 사용한다.

---

### \(\lambda = 3\)의 경우

\(\lambda = 3\)은 7중해이다.

고유벡터가 두 개만 구해졌다고 하면,  
요르단 셀은 두 개가 된다.

또 전체 크기의 합은 7이어야 한다.

즉 가능한 구조는 예를 들어 다음과 같다.

$$
5 + 2 = 7
$$

또는

$$
4 + 3 = 7
$$

또는

$$
6 + 1 = 7
$$

이런 식으로 여러 가능성이 있다.

---

### 어떤 구조가 맞는지 판정하는 법

요르단 셀 구조를 판정하기 위해 다음 값을 본다.

$$
K_i = \dim \operatorname{Ker}(A - 3I)^i
$$

즉,

$$
(A - 3I)^i p = 0
$$

이 되는 벡터 공간의 차원을 확인한다.

이 값이 어떻게 증가하는지를 보면  
요르단 셀의 크기를 알 수 있다.

예를 들어 책에서는 후보를 비교해서  
어떤 요르단 구조가 맞는지 판정한다.

---

### 사슬 구성하기

예를 들어 \(\lambda = 3\)에 대해 길이 5짜리 사슬을 만들면,

먼저 가장 끝 벡터 \(p_5\)를 구한다.

$$
(A - 3I)^5p_5 = 0
$$

그다음 차례대로 정의한다.

$$
p_4 = (A - 3I)p_5
$$

$$
p_3 = (A - 3I)p_4
$$

$$
p_2 = (A - 3I)p_3
$$

$$
p_1 = (A - 3I)p_2
$$

그러면 다음과 같은 사슬이 된다.

$$
p_5 \rightarrow p_4 \rightarrow p_3 \rightarrow p_2 \rightarrow p_1 \rightarrow 0
$$

---

### 주의할 점

중간에 어떤 벡터가 0이 되면 실패다.

예를 들어 \(p_4, p_3, p_2, p_1\) 중 하나가 0이 되면  
원하는 길이의 사슬이 만들어지지 않는다.

또 이미 구한 벡터들과 선형종속이 되어도 안 된다.

이런 경우에는 처음 고른 \(p_5\)를 다시 선택해야 한다.

책에서는 이런 경우를  
“절묘하게 운이 나쁜 예외”라고 설명한다.

---

### 최종적으로 하는 일

모든 고유값에 대해 필요한 벡터들을 구한다.

예제에서는 다음과 같이 벡터들을 모은다.

$$
P = (p_1, p_2, \dots, p_{11})
$$

그러면

$$
P^{-1}AP
$$

가 요르단 표준형 \(J\)가 된다.

---

### 핵심 요약

요르단 표준형을 구할 때는

1. 고유값을 구한다.
2. 각 고유값의 중복도를 확인한다.
3. 고유벡터 개수를 확인한다.
4. 부족하면 일반화 고유벡터를 구한다.
5. 벡터들을 사슬 형태로 연결한다.
6. 그 벡터들을 모아 \(P\)를 만든다.
7. 그러면 \(P^{-1}AP = J\)가 된다.

요르단 표준형은 결국  
고유벡터와 일반화 고유벡터를 잘 배열해서  
복잡한 행렬을 최대한 단순한 블록 구조로 바꾸는 방법이다.

### 7.6 요단 표준형을 변환할 수 있는 것의 증명
- ### 요르단 표준형으로 변환할 수 있는 조건

정방행렬 \(A\)에 대해

$$
P^{-1}AP
$$

가 요르단 표준형이 되게 하려면, 적절한 정칙행렬 \(P\)가 필요하다.

이때 \(P\)는 벡터들

$$
p_1, p_2, \dots, p_n
$$

을 열벡터로 모아 만든 행렬이다.

---

### 필요한 벡터들의 조건

\(p_1, \dots, p_n\)은 다음 조건을 만족해야 한다.

#### 사슬 구조를 이룬다

각 벡터는 어떤 고유값 \(\lambda\)에 대해

$$
(A-\lambda I)
$$

를 반복해서 곱하면 결국 영벡터가 되는 구조를 가져야 한다.

즉, 다음과 같은 계열을 만든다.

$$
p_k \rightarrow p_{k-1} \rightarrow \cdots \rightarrow p_1 \rightarrow 0
$$

이런 계열이 요르단 셀 하나에 대응한다.

---

#### 선형독립이어야 한다

모든 벡터

$$
p_1, \dots, p_n
$$

은 선형독립이어야 한다.

그래야 이 벡터들이 기저를 이루고,  
이들을 모은 행렬 \(P\)가 역행렬을 가질 수 있다.

즉,

$$
P = (p_1, p_2, \dots, p_n)
$$

가 정칙행렬이어야 한다.

---

### 유파 \(W(\lambda)\)

특정 고유값 \(\lambda\)에 대해,

$$
(A-\lambda I)
$$

를 여러 번 곱했을 때 결국 \(0\)이 되는 벡터들을 모은 집합을 책에서는 임시로 **유파 \(W(\lambda)\)** 라고 부른다.

예를 들어 \(W(3)\)은

$$
(A-3I)
$$

를 몇 번 곱하면 \(0\)이 되는 벡터들의 집합이다.

---

### 유파에 속하는 벡터

고유벡터는 당연히 유파에 속한다.

왜냐하면 고유벡터 \(p\)는

$$
(A-\lambda I)p = 0
$$

을 만족하기 때문이다.

일반화 고유벡터도 유파에 속한다.

예를 들어

$$
(A-\lambda I)^k p = 0
$$

이 되는 벡터라면,  
몇 번 반복해서 \(A-\lambda I\)를 곱하면 \(0\)이 되므로  
\(W(\lambda)\)에 속한다.

---

### 유파에 속하지 않는 벡터

어떤 \(\lambda\)에 대해서도

$$
(A-\lambda I)^k p = 0
$$

이 되지 않는 벡터는 후보가 될 수 없다.

즉, 요르단 표준형을 만들기 위한 \(P\)의 열벡터로 사용할 수 없다.

---

### 핵심 요약

요르단 표준형을 만들려면  
벡터 \(p_1, \dots, p_n\)을 잘 골라야 한다.

이 벡터들은

- 어떤 고유값 \(\lambda\)에 대한 사슬 구조를 이루고
- 모두 선형독립이어야 하며
- 각 고유값의 유파 \(W(\lambda)\)에 속해야 한다.

이 조건을 만족하면

$$
P^{-1}AP
$$

가 요르단 표준형이 된다.

## [유파의 크기에 대해]
- 0 이외의 멤버가 있는 유파 \(W(\lambda)\)는 \lambda가 A의 고윳값인 것 뿐

### 유파에서 필요한 벡터를 고르는 과정

고유값마다 유파 \(W(\lambda)\)가 있다.

유파 \(W(\lambda)\)는

$$
(A-\lambda I)^k p = 0
$$

이 되는 벡터들의 모임이다.

즉, 어떤 벡터에 \((A-\lambda I)\)를 여러 번 곱했을 때 결국 \(0\)이 되면 그 벡터는 \(W(\lambda)\)에 속한다.

---

### 유파에서 몇 개의 벡터를 골라야 할까?

고유값 \(\lambda\)가 특성방정식의 \(k\)중해라면,  
그 유파 \(W(\lambda)\) 안에서 \(k\)개의 선형독립인 벡터를 고를 수 있다.

예를 들어 고유값 \(7\)이 3중해라면,

$$
W(7)
$$

안에서 선형독립인 벡터 3개를 고를 수 있다.

---

### 하나씩 고유값을 분리하는 아이디어

고유값 \(7\)에 대한 고유벡터 하나를 먼저 고른다.

그 벡터를 첫 번째 열로 하는 행렬 \(P_1\)을 만들면,

$$
P_1^{-1}AP_1
$$

은 다음과 비슷한 형태가 된다.

$$
\begin{bmatrix}
7 & ? & \cdots & ? \\
0 &  &  &  \\
\vdots &  & A_1 &  \\
0 &  &  &
\end{bmatrix}
$$

여기서 첫 번째 열 아래가 전부 \(0\)이 되는 것이 중요하다.

---

### 왜 이렇게 하냐면

첫 번째 고유벡터를 분리하면,  
남은 부분 \(A_1\)만 다시 살펴보면 된다.

이때 원래 고유값 \(7\)이 3중해였다면,  
\(A_1\)에서는 고유값 \(7\)이 2중해로 줄어든다.

즉,

$$
3중해 \rightarrow 2중해 \rightarrow 1중해
$$

처럼 하나씩 줄여가며 반복할 수 있다.

---

### 반복 과정

첫 번째 단계:

$$
A \rightarrow A_1
$$

두 번째 단계:

$$
A_1 \rightarrow A_2
$$

세 번째 단계:

$$
A_2 \rightarrow A_3
$$

이렇게 반복하면 고유값 \(7\)에 해당하는 벡터들을 필요한 개수만큼 얻을 수 있다.

---

### 최종 결과

이 과정을 통해 행렬 \(Q\)를 만들면,

$$
Q^{-1}AQ = U
$$

와 같은 형태가 된다.

여기서 \(U\)는 위쪽 삼각행렬 형태이고,  
대각선에는 고유값들이 놓인다.

예를 들어 고유값 \(7\)이 3중해라면,

$$
\begin{bmatrix}
7 & ? & ? & \cdots \\
0 & 7 & ? & \cdots \\
0 & 0 & 7 & \cdots \\
0 & 0 & 0 & \cdots
\end{bmatrix}
$$

처럼 앞쪽에 \(7\)이 세 번 나타난다.

---

### 중요한 결론

이렇게 얻은 열벡터들을

$$
q_1, q_2, q_3, \dots
$$

라고 하면,

$$
q_1, q_2, q_3
$$

은 모두 \(W(7)\)에 속한다.

즉, 각각에 대해

$$
(A-7I)^k q = 0
$$

이 되는 어떤 \(k\)가 존재한다.

---

### 핵심 요약

고유값 \(\lambda\)가 \(k\)중해라면,  
유파 \(W(\lambda)\) 안에서 \(k\)개의 선형독립 벡터를 고를 수 있다.

이 벡터들을 이용하면 행렬 \(A\)를 점점 단순한 형태로 바꿀 수 있고,  
최종적으로 요르단 표준형을 만들기 위한 기저를 구성할 수 있다.